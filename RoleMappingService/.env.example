# RoleMappingService environment example
# NOTE: Services should read from process.env; do not commit real secrets.

# Port for the Node service
PORT=4001

# Internal service token required via X-Internal-Token header
INTERNAL_TOKEN=dev-internal-token
