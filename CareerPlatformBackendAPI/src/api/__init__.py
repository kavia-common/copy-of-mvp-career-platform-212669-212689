"""
API package for the Career Platform Backend.

This module exists to mark the API package and provide a clear place for
router modules (e.g., users, roles) and application setup (see src/api/main.py).
"""
